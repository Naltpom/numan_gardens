{% extends "base.html.twig" %}

{% block h1 %}
        {%  if titre is defined %}
{{ titre }}

{% else %}
        Ajouter un produit
{% endif %}

{% endblock %}


{% block body  %}

  {# {{ form(form)}}#}
<div class="row justify-content-center" >
        <div class="col-lg-6">


                {% if produit is defined %}
                {% for image in produit.image %}
                {% if produit.image is not null %}
                        <img src="{{ asset('img/' ~ image.image01) }}" style=" min-width:50%; height:600px;">  

                {% endif %} 
                {% endfor %}
                {% endif %}
        </div>

         <div class="col-lg-4">

                        {{ form_start(form) }}
                        {{ form_row(form.titre) }}
                        {{ form_row(form.description) }}
                        {{ form_row(form.reference) }}
                        {{ form_row(form.volume) }}
                        {{ form_row(form.stock) }}
                        {{ form_row(form.certificat) }}
                        {{ form_row(form.categorie) }}
                        {{ form_row(form.prix) }}
                        
       {{  form_end(form) }}
        </div>
</div>


        <div class="col-lg-2">
                {{ form_row(form.enregistrer, { 'label': bouton }) }}
                 
        </div>
        <div class="col-lg-2">
               <a href="{{ path("produit") }}" class="btn btn-danger">annuler</a>
        </div>



               


{% endblock %}



